/**
 * @prettier
 */

/**
 * Redux actions for app.
 */
class AppActions {
  static setIsUserLoggedOut(state) {
    return async dispatch => {
      //    dispatch(setIsUserLoggedOut(state));
    };
  }
}

export default AppActions;
